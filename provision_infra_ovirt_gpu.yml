- name: deploy cluster on ovirt with gpu
  hosts: localhost
  become: no
  gather_facts: no
  connection: local
  vars_files:
    - node-config/nodes-ovirt-gpu-test3.yml
  roles:
    - ansible-role-ovirt